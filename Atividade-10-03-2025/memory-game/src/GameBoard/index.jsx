import imagens from '../public'

function GameBoard() {
    return (
        <header className={styles.GameBoard}>
            <span>RemuloBorges.dev</span>
        </header>
    )    
}

export default GameBoard